{% extends 'layout.twig' %}

{% block content %}
    {% for book in books %}
        <h2><a href="{{ path_for('book_view', { id: book.id }) }}">{{ book.title }}</a></h2>
        <span>{{ book.year|date('Y') }}</span>
    {% endfor %}
{% endblock %}